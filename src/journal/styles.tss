Screen {
    layout: vertical;
}

#main {
    width: 1fr;
    height: 1fr;
}

Prompt {
    align: center middle;
}

#dialog {
    width: 60;
    height: auto;
    padding: 1 2;
    border: none;
    background: $surface;
}